<p>This directory contains subclasses of <code>tensorflow::profiler::ProfilerInterface</code>
and related code.</p>
<p><code>tensorflow::ProfilerSession</code> manages a collection of profile sources. Each
profile source implements a subclass of
<code>tensorflow::profiler::ProfilerInterface</code>.</p>